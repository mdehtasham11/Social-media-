const config = {
    API_BASE_URL: 'http://localhost:8002/api', // Local development server
};

export default config; 
// const config = {
//     API_BASE_URL:
//       import.meta.env.PROD
//         ? "https://meet-main.vercel.app/api"   // Vercel URL
//         : "http://localhost:8002/api"          // local dev
//   };
//   export default config;